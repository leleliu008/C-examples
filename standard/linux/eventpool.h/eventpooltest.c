#include <linux/eventpoll.h>
#include <sched.h>

int main() {
    //__force __poll_t a;

    cpu_set_t a;

    //#if (NGX_READ_EVENT != (EPOLLIN|EPOLLRDHUP))
    //    return 1;
    //#endif

    return 0;
}
