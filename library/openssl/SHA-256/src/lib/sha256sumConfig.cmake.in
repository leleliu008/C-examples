@PACKAGE_INIT@

set(SHA256SUM_VERSION       "@PROJECT_VERSION@")
set(SHA256SUM_VERSION_MAJOR "@PROJECT_VERSION_MAJOR@")
set(SHA256SUM_VERSION_MINOR "@PROJECT_VERSION_MINOR@")
set(SHA256SUM_VERSION_PATCH "@PROJECT_VERSION_PATCH@")

set_and_check(SHA256SUM_INSTALL_DIR "${PACKAGE_PREFIX_DIR}")
set_and_check(SHA256SUM_INCLUDE_DIR "${PACKAGE_PREFIX_DIR}/include")
set_and_check(SHA256SUM_LIBRARY_DIR "${PACKAGE_PREFIX_DIR}/lib")

include("${CMAKE_CURRENT_LIST_DIR}/sha256sumTargets.cmake")
