@PACKAGE_INIT@

set(UNTAR_VERSION       "@PROJECT_VERSION@")
set(UNTAR_VERSION_MAJOR "@PROJECT_VERSION_MAJOR@")
set(UNTAR_VERSION_MINOR "@PROJECT_VERSION_MINOR@")
set(UNTAR_VERSION_PATCH "@PROJECT_VERSION_PATCH@")

set_and_check(UNTAR_INSTALL_DIR "${PACKAGE_PREFIX_DIR}")
set_and_check(UNTAR_INCLUDE_DIR "${PACKAGE_PREFIX_DIR}/include")
set_and_check(UNTAR_LIBRARY_DIR "${PACKAGE_PREFIX_DIR}/lib")

include("${CMAKE_CURRENT_LIST_DIR}/untarTargets.cmake")
