@PACKAGE_INIT@

set(LIBHTTP_VERSION       "@PROJECT_VERSION@")
set(LIBHTTP_VERSION_MAJOR "@PROJECT_VERSION_MAJOR@")
set(LIBHTTP_VERSION_MINOR "@PROJECT_VERSION_MINOR@")
set(LIBHTTP_VERSION_PATCH "@PROJECT_VERSION_PATCH@")

set_and_check(LIBHTTP_INSTALL_DIR "${PACKAGE_PREFIX_DIR}")
set_and_check(LIBHTTP_INCLUDE_DIR "${PACKAGE_PREFIX_DIR}/include")
set_and_check(LIBHTTP_LIBRARY_DIR "${PACKAGE_PREFIX_DIR}/lib")

include("${CMAKE_CURRENT_LIST_DIR}/httpTargets.cmake")
